# 京学龄·差异审计本地网站

该项目和原“京学龄”网站相互独立，不会修改原网站文件。

## 启动

双击项目根目录的 `启动本地网站.command`，浏览器会打开：

`http://localhost:4174/`

关闭启动网站的终端窗口即可停止服务。原网站仍使用 `http://localhost:4173/`，两个网站可以同时运行。

## 离线范围

- 关键差异、口径表、8 篇文章筛选与研究方法：离线可用。
- Excel 底表下载：离线可用。
- 微信文章和政府原始来源：只有主动点击时才联网。

首次在新电脑运行需要执行 `npm ci` 安装依赖。
