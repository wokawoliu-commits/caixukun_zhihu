# 张月跨平台统一调研静态站点（引用更新版）

该目录已经可以直接作为静态网站根目录部署。

## 文件

- `index.html`：网站入口，包含完整报告、图表、表格和来源查看功能。
- `05_微博与小红书引用来源.md`：30条微博与35条小红书逐项引用及备用入口。
- `.nojekyll`：避免 GitHub Pages 对静态文件执行 Jekyll 处理。

## 部署参数

- 构建命令：无需构建
- 发布/输出目录：当前目录（`.`）
- 入口文件：`index.html`
- 外部依赖：无

报告内容快照日期为 2026-08-31。本目录是新增引用与小红书URL策略更新版；原始版本继续保留在相邻的 `zhangyue1` 目录中，不应被本版本覆盖。正文使用小红书 `discovery/item/{noteId}` 查看入口；遇到扫码或不可用页时，可使用引用附录中的标题搜索、RedNote链接和笔记ID复核。

`index.html` 不依赖外部图片、JavaScript、CSS或数据文件；但应同时部署引用附录，以保证报告中的完整来源入口可用。
