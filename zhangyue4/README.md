# SNACKPAPER × 张月《下一拍，由我分享》

该目录为独立的新版本，不覆盖`zhangyue1`、`zhangyue2`或既有调研文件。

## 文件

- `index.html`：完整拍摄与整合营销方案，可直接作为静态网站入口。
- `artifact.json`：生成HTML所用的规范化报告数据。
- `source_notes/`：PDF逻辑拆解、张月调研提炼及策划推导与待确认事项。
- `.nojekyll`：静态部署标记。

## 部署

- 无需构建。
- 发布目录为当前目录（`.`）。
- 入口为`index.html`。
- `index.html`为自包含文件，不依赖CDN、远程脚本或侧车数据。

